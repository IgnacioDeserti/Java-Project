/**
 * Mirrors the Card.Label enum on the backend. Order matters: it's the order the picker
 * and the filter bar render, and the order the API sorts a card's labels into.
 */
export const LABELS = ["GREEN", "BLUE", "PURPLE", "ORANGE", "RED", "GRAY"];
